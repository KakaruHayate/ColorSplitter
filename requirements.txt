# The dependency list lives in pyproject.toml. This file only exists so the
# familiar command keeps working:
#
#     pip install -r requirements.txt
#
# For a torch-free install (timbre / speaker encoders only), use:
#
#     pip install -e .
#
-e .[all]
